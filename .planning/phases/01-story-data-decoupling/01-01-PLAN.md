---
phase: 01-story-data-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
autonomous: true
requirements:
  - STORY-01

must_haves:
  truths:
    - "PersonStory renders the callout summary from person.summary, not from person.id conditionals"
    - "Changing a person's id in HOSPITAL_PEOPLE does not break or blank their story display"
    - "A person object with no summary field hides the callout box instead of rendering blank text"
  artifacts:
    - path: "src/App.jsx"
      provides: "HOSPITAL_PEOPLE with summary field, PersonStory reading from data"
      contains: "summary:"
  key_links:
    - from: "HOSPITAL_PEOPLE[].summary"
      to: "PersonStory callout <p>"
      via: "person.summary rendered in JSX"
      pattern: "person\\.summary"
---

<objective>
Decouple PersonStory from hard-coded person IDs by moving per-person callout summary text into the HOSPITAL_PEOPLE data array and rendering it from the data field.

Purpose: After this change, PersonStory has zero knowledge of specific person IDs. Story content is entirely data-driven, enabling Phase 4 to swap real patient stories by changing data only.
Output: Modified src/App.jsx with summary field on each person object and PersonStory reading from that field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-story-data-decoupling/01-CONTEXT.md
@src/App.jsx
</context>

<interfaces>
<!-- Current HOSPITAL_PEOPLE shape (lines 200-205 of src/App.jsx): -->
<!-- Each object: {id, name, role, x, y, color, age, bg, timeline: [...]} -->
<!-- After this plan, each object gains: summary: "string" -->

<!-- Current PersonStory callout (lines 542-549 of src/App.jsx): -->
<!-- Uses person.id === "maria", person.id === "dr-okafor", etc. to pick summary text -->
<!-- After this plan: renders person.summary directly, guards with falsy check -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add summary field to HOSPITAL_PEOPLE data</name>
  <files>src/App.jsx</files>
  <read_first>src/App.jsx (lines 200-205 for HOSPITAL_PEOPLE, lines 542-548 for current summary text)</read_first>
  <action>
Add a `summary` string field to each of the 4 person objects in the `HOSPITAL_PEOPLE` array (lines 200-205 of src/App.jsx). The summary text comes from the existing hard-coded conditionals at lines 544-547. Place `summary` after the `bg` field and before the `timeline` field on each object, following the existing compact single-line-per-person style.

Exact values to add (per D-01, D-02 -- simple string field on person object):

For the person with id "maria", add:
summary:"Her son got screened because parents organized. Her housing got fixed because she testified. Her dental care stalled because nobody voted."

For the person with id "dr-okafor", add:
summary:"She came home to serve her community. Low reimbursement rates \u2014 set without local input \u2014 mean she carries triple the recommended patient load."

For the person with id "james", add:
summary:"He almost lost years of his life to a coverage gap. A navigator program, funded through local advocacy, caught him in time."

For the person with id "sofia", add:
summary:"She waited 8 months for a $120 filling. The ER visit cost $1,800. The dental program died in an election with 6% turnout."

The em dash in Dr. Okafor's summary uses the Unicode character \u2014 (matching the existing text at line 545 which reads "set without local input"). Preserve the existing compact code style -- the summary field is added inline on the same line as all other fields for that person object.
  </action>
  <verify>
    <automated>grep -c 'summary:' src/App.jsx | grep -q '4' && echo "PASS: 4 summary fields found" || echo "FAIL: expected 4 summary fields"</automated>
  </verify>
  <acceptance_criteria>
- src/App.jsx contains exactly 4 occurrences of `summary:` inside HOSPITAL_PEOPLE entries
- The maria object contains summary:"Her son got screened because parents organized
- The dr-okafor object contains summary:"She came home to serve her community
- The james object contains summary:"He almost lost years of his life
- The sofia object contains summary:"She waited 8 months for a $120 filling
- No other structural changes to HOSPITAL_PEOPLE (id, name, role, x, y, color, age, bg, timeline fields unchanged)
  </acceptance_criteria>
  <done>All 4 person objects in HOSPITAL_PEOPLE have a summary string field containing their callout text</done>
</task>

<task type="auto">
  <name>Task 2: Replace person.id conditionals with person.summary in PersonStory</name>
  <files>src/App.jsx</files>
  <read_first>src/App.jsx (lines 512-553 for PersonStory component, especially lines 542-549 for the callout box)</read_first>
  <action>
In the PersonStory component (starting around line 512 of src/App.jsx), modify the callout box section (the div with background:"#1a1a1a" starting around line 542).

Replace the four person.id conditional lines (lines 544-547):
```
{person.id==="maria"&&"Her son got screened because parents organized..."}
{person.id==="dr-okafor"&&"She came home to serve her community..."}
{person.id==="james"&&"He almost lost years of his life..."}
{person.id==="sofia"&&"She waited 8 months for a $120 filling..."}
```

With a single expression that reads from the data field:
```
{person.summary}
```

Additionally, wrap the entire callout box div (the div with background:"#1a1a1a") in a conditional guard so it only renders when person.summary is truthy (per D-03 -- hide callout box if summary is falsy). Use the pattern:

```
{person.summary&&(
  <div style={{background:"#1a1a1a",color:"#faf8f3",padding:"28px 24px",marginTop:32,textAlign:"center",border:`1px solid ${RED}`}}>
    <p style={{fontFamily:"'Libre Franklin',sans-serif",fontSize:16,fontWeight:700,lineHeight:1.45,marginBottom:10}}>
      {person.summary}
    </p>
    <p style={{fontFamily:"'Libre Franklin',sans-serif",fontSize:12,color:"#9ca3af"}}>Civic engagement isn't abstract — it is the mechanism by which need becomes care.</p>
  </div>
)}
```

The shared tagline ("Civic engagement isn't abstract...") stays hardcoded as a constant string inside the component (per D-01 -- universal message, not per-person).

After this change, PersonStory must contain zero occurrences of `person.id===` or `person.id ===`.
  </action>
  <verify>
    <automated>grep -c 'person\.id===' src/App.jsx | grep -q '0' && grep -q 'person\.summary' src/App.jsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- src/App.jsx contains zero occurrences of the string `person.id===`
- src/App.jsx contains the expression `person.summary` inside the PersonStory component
- src/App.jsx contains the guard pattern `person.summary&&` before the callout div
- The shared tagline string "Civic engagement isn't abstract" remains present in PersonStory as a hardcoded constant
- `npm run build` exits with code 0 (no build errors)
- `npm run lint` exits with code 0 (no lint errors)
  </acceptance_criteria>
  <done>PersonStory renders callout text from person.summary with no person.id conditionals. Build and lint pass. Falsy summary hides the entire callout box.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Zero person.id conditionals: `grep -c 'person\.id===' src/App.jsx` returns 0
2. Summary field present on all 4 people: `grep -c 'summary:' src/App.jsx` returns 4
3. PersonStory reads from data: `grep 'person\.summary' src/App.jsx` matches at least 2 lines (the guard and the render)
4. Build succeeds: `npm run build` exits 0
5. Lint passes: `npm run lint` exits 0
6. Robustness check: Mentally confirm that if any person's `id` field were changed (e.g., "maria" -> "maria-santos"), the summary would still render because it comes from `person.summary`, not from an id match
</verification>

<success_criteria>
- PersonStory renders callout summary text from `person.summary` data field, not from `person.id` conditionals
- All 4 HOSPITAL_PEOPLE entries have a `summary` string field with their respective callout text
- Changing any person's `id` value would not affect their story display
- A person with no `summary` field would see the callout box hidden (not blank)
- The shared tagline remains hardcoded in the component
- Production build and lint both pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-story-data-decoupling/01-01-SUMMARY.md`
</output>
