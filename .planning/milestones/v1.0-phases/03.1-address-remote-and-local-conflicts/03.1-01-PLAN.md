---
phase: 03.1-address-remote-and-local-conflicts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [src/App.jsx]
autonomous: true
requirements: [HOSP-01, HOSP-02, HOSP-03, STORY-01]

must_haves:
  truths:
    - "Hospital scene renders four rooms using remote's widened geometry (w:36, x:11/53)"
    - "Person figures sit centered in their rooms using recalculated coordinates for new geometry"
    - "PersonStory displays summary callout boxes for all four people (Phase 1 summary fields preserved)"
    - "Department text uses remote's smaller font sizes and shortened labels"
    - "Touch targets and touch event handlers from Phase 3 remain functional"
    - "NavBar shows three tab buttons (US Map, NYC Map, Health Center) and Story breadcrumb at level 3"
  artifacts:
    - path: "src/App.jsx"
      provides: "Merged hospital scene with remote geometry/content + local technical features"
      contains: "w:36,h:28"
  key_links:
    - from: "DEPARTMENTS constant"
      to: "HospitalScene rendering"
      via: "DEPARTMENTS.map in SVG"
      pattern: "DEPARTMENTS\\.map"
    - from: "HOSPITAL_PEOPLE constant"
      to: "PersonStory summary callout"
      via: "person.summary conditional"
      pattern: "person\\.summary"
    - from: "HOSPITAL_PEOPLE coordinates"
      to: "touch target rect positioning"
      via: "p.x-6, p.y-2 offset"
      pattern: "p\\.x-6"
---

<objective>
Merge remote branch content and geometry changes into local codebase while preserving Phase 1 summary fields and Phase 3 touch infrastructure.

Purpose: Resolve git divergence between local (Phase 3 execution) and remote (`7c3f536 "end changes"`) so development can continue on a unified codebase.
Output: Single merged `src/App.jsx` with remote's redesigned geometry/content/styling and local's technical features intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-address-remote-and-local-conflicts/03.1-CONTEXT.md
@.planning/phases/03-hospital-scene-layout/03-01-SUMMARY.md
@src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge data constants (DEPARTMENTS, HOSPITAL_PEOPLE, CHAPTERS)</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (lines 200-310 — current HOSPITAL_PEOPLE, DEPARTMENTS, CHAPTERS)
    - .planning/phases/03.1-address-remote-and-local-conflicts/03.1-CONTEXT.md (canonical values)
  </read_first>
  <action>
Update three data constants in src/App.jsx. All values below are exact — copy verbatim.

**1. DEPARTMENTS constant (replace entire array):**
Replace the current DEPARTMENTS array with these four objects. Note the reordered bottom row (casemanagement is now bottom-left, maternal is bottom-right):
```
{id:"hivid",label:"Infectious Disease",x:11,y:22,w:36,h:28,metric:"PROVIDERS",metricLabel:" ",severity:"high",note:"Care = survival"},
{id:"insurance",label:"Insurance Navigation",x:53,y:22,w:36,h:28,metric:"CONTRACTS",metricLabel:" ",severity:"critical",note:"Network gaps cost lives"},
{id:"casemanagement",label:"Benefits & Housing",x:11,y:55,w:36,h:28,metric:"MEDICAID",metricLabel:" ",severity:"high",note:"Coverage gaps = discharge"},
{id:"maternal",label:"Maternal & HIV Care",x:53,y:55,w:36,h:28,metric:"NY vs. NC",metricLabel:" ",severity:"medium",note:"Laws determine access"},
```

**2. HOSPITAL_PEOPLE — update coordinates, names, bg text, keep summary fields:**

For each person, apply ALL of these changes:

dr-saffo:
- role: `"Healthcare Provider, HIV Specialist"` (was "HIV & Infectious Disease Physician")
- x:29, y:38 (center of hivid room: 11 + 36/2 = 29, y = 22 + 16 = 38)
- bg: `"A HIV specialist at a health center in New York, Dr. Saffo cares for some of the city's most vulnerable patients, navigating daily the intersection of public health, insurance policy, and human dignity. These are her patients' stories."` (note: "A HIV" not "An HIV", comma not em-dash before "navigating", "a health center" not "a community health center")
- KEEP summary field exactly as-is from local
- timeline[1].detail: `"Healthcare providers like Dr. Saffo..."` (was "Physicians like Dr. Saffo...")
- timeline[2].detail: replace em-dashes with commas: `"The difference between New York and North Carolina, in housing benefits for people living with HIV, is not an abstraction. It is whether a mother and her unborn child have a roof."`
- timeline[3].detail: replace em-dash with period: `"When a Medicaid benefit limit ends a life, it is not a medical failure. It is a policy failure. One made far from the hospital bed, in rooms shaped by who votes and who shows up."`

patient-one:
- name: `"A Man With Pancreatitis"` (was "Patient Story: A Man With Pancreatitis")
- x:71, y:38 (center of insurance room: 53 + 36/2 = 71, y = 22 + 16 = 38)
- KEEP summary field exactly as-is from local
- No bg or timeline text changes

patient-two:
- name: `"A Young Man Diagnosed"` (was "Patient Story: A Young Man Newly Diagnosed")
- x:29, y:71 (center of casemanagement room: 11 + 36/2 = 29, y = 55 + 16 = 71)
- bg: replace em-dash with comma: `"Diagnosed with HIV in his mid-20s, rare today, when PrEP has made..."` (comma after "mid-20s" instead of em-dash)
- KEEP summary field exactly as-is from local
- timeline[0].detail: replace em-dash with comma: `"...He was shaken, despondent, but engaging with his care and showing up."` (comma after "despondent" instead of em-dash)

patient-three:
- name: `"Pregnant Woman With HIV"` (was "Patient Story: A Pregnant Woman With HIV")
- x:71, y:71 (center of maternal room: 53 + 36/2 = 71, y = 55 + 16 = 71)
- bg: replace em-dash with comma: `"...New York State's benefits, tied to HIV status and pregnancy, gave her something her home state never had: a home."` (commas around "tied to HIV status and pregnancy" instead of em-dashes)
- KEEP summary field exactly as-is from local
- timeline[1].detail: replace em-dash with comma: `"...The odds were steep, but New York has policies that other states do not."` (comma after "steep" instead of em-dash)
- timeline[2].detail: replace em-dash with comma: `"...tied to HIV status and pregnancy, a combination of protections..."` (comma instead of em-dash)
- timeline[3].detail: replace em-dashes with commas: `"...What feels like luck is actually policy, and policy is shaped by who votes, who testifies, and who organizes."` (comma instead of em-dash before "and policy")

**3. CHAPTERS — update Chapter 3 body text:**
Change the Chapter 3 body from `"...who gets housing — these determine..."` to `"...who gets housing. These determine..."` (period + capital T instead of em-dash + lowercase t).

Full Chapter 3 body: `"Every story in this health center shows how policy shapes survival. Medicaid benefit limits, hospital-insurer contract disputes, and state-level decisions about who gets housing. These determine who lives, who loses their provider, and who finds a home. Dr. Stella Saffo witnesses these outcomes daily."`
  </action>
  <verify>
    <automated>cd /home/jaeho/civic-health-alliance && grep -c 'w:36,h:28' src/App.jsx | grep -q '4' && grep -c 'summary:' src/App.jsx | grep -q '4' && grep 'x:29,y:38' src/App.jsx | grep -q 'dr-saffo' && grep 'x:71,y:38' src/App.jsx | grep -q 'patient-one' && grep 'x:29,y:71' src/App.jsx | grep -q 'patient-two' && grep 'x:71,y:71' src/App.jsx | grep -q 'patient-three' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'w:36,h:28' src/App.jsx returns exactly 4 matches (all four departments use w:36)
    - grep 'x:11,y:22' src/App.jsx matches hivid department
    - grep 'x:53,y:22' src/App.jsx matches insurance department
    - grep 'x:11,y:55' src/App.jsx matches casemanagement department
    - grep 'x:53,y:55' src/App.jsx matches maternal department
    - grep 'label:"Infectious Disease"' src/App.jsx returns 1 match (shortened from "HIV / Infectious Disease")
    - grep 'metric:"PROVIDERS"' src/App.jsx returns 1 match (was "3 providers")
    - grep 'note:"Care = survival"' src/App.jsx returns 1 match (shortened from "Continuity of care = survival")
    - grep 'note:"Laws determine access"' src/App.jsx returns 1 match (shortened from "State law determines access")
    - grep 'x:29,y:38' src/App.jsx matches dr-saffo entry
    - grep 'x:71,y:38' src/App.jsx matches patient-one entry
    - grep 'x:29,y:71' src/App.jsx matches patient-two entry
    - grep 'x:71,y:71' src/App.jsx matches patient-three entry
    - grep 'summary:' src/App.jsx returns exactly 4 matches (all four people retain summary fields)
    - grep 'name:"A Man With Pancreatitis"' src/App.jsx returns 1 match (no "Patient Story:" prefix)
    - grep 'name:"A Young Man Diagnosed"' src/App.jsx returns 1 match (no "Newly")
    - grep 'name:"Pregnant Woman With HIV"' src/App.jsx returns 1 match (no "A" prefix, no "Patient Story:")
    - grep 'role:"Healthcare Provider, HIV Specialist"' src/App.jsx returns 1 match
    - grep 'who gets housing. These determine' src/App.jsx returns 1 match (Chapter 3 text)
    - grep 'A HIV specialist at a health center' src/App.jsx returns 1 match (dr-saffo bg)
    - casemanagement appears BEFORE maternal in DEPARTMENTS array order (bottom-left, bottom-right)
  </acceptance_criteria>
  <done>All three data constants updated: DEPARTMENTS uses remote geometry (w:36, x:11/53, reordered bottom row), HOSPITAL_PEOPLE has recalculated coordinates + remote content + preserved summary fields, CHAPTERS has remote text change.</done>
</task>

<task type="auto">
  <name>Task 2: Merge component rendering (HospitalScene styling, NavBar redesign)</name>
  <files>src/App.jsx</files>
  <read_first>
    - src/App.jsx (lines 522-735 — current HospitalScene, NavBar, App components)
    - .planning/phases/03.1-address-remote-and-local-conflicts/03.1-CONTEXT.md (styling values and NavBar changes)
  </read_first>
  <action>
Update HospitalScene and NavBar components in src/App.jsx. Preserve all Phase 3 technical features (touch targets, touch handlers).

**1. HospitalScene — adopt remote styling, keep local touch infrastructure:**

Floor markers: Change x={9} to x={2.5} on both floor marker text elements:
- `<text x={2.5} y={19} ...>2F</text>` (was x={9})
- `<text x={2.5} y={57} ...>1F</text>` (was x={9})

Department text font sizes (in the DEPARTMENTS.map rendering):
- Label fontSize: change 2.2 to 1.8 (the `d.label` text at `d.y+4.5`)
- Metric fontSize: change 3.2 to 2.8 (the `d.metric` text at `d.y+9`)
- MetricLabel fontSize: change 1.5 to 1.3 (the `d.metricLabel` text at `d.y+12`)
- Note fontSize: change 1.6 to 1.3 (the `d.note` text at `d.y+d.h-2.5`)

KEEP these Phase 3 features untouched:
- `onTouchEnd={()=>onHoverDept(null)} onTouchCancel={()=>onHoverDept(null)}` on department g elements
- `<rect x={p.x-6} y={p.y-2} width={12} height={12} fill="transparent"` touch target rects
- `onTouchEnd={()=>onHoverPerson(null)} onTouchCancel={()=>onHoverPerson(null)}` on person g elements

Hover tooltip (the `{hoveredPerson===p.id&&<g>...}` block):
- Background rect: change from `x={p.x-18} y={p.y-16} width={36} height={8} rx={1} fill="rgba(26,26,26,0.93)"` to `x={p.x-35} y={p.y-18} width={70} height={12} rx={1.5} fill="rgba(26,26,26,0.95)"`
- Name text y: change from `y={p.y-12}` to `y={p.y-13}` (keep fontSize 2.2)
- Role text fontSize: change from 1.6 to 1.4 (keep y={p.y-9.5})

**2. NavBar — adopt remote redesign:**

Replace the entire NavBar function with:
- Add `onGoHospital` to destructured props: `function NavBar({level,onHome,onGoUS,onGoNYC,onGoHospital}){`
- Change `nycActive` from `level>=1` to `level===1`
- Add `const hospitalActive = level >= 2;`
- In mapBtnBase style object:
  - Remove `width:208`
  - Add `padding:"0 20px"`
  - Change fontSize from 16 to 15
- In the outer div style:
  - Change `padding:"0 20px"` to `padding:"0 16px"`
  - Add `overflowX:"auto"` and `whiteSpace:"nowrap"`
- Home button:
  - Change text from `"Civic Health Alliance"` to `"Civic Health"`
  - Change `marginRight:20` to `marginRight:16`
  - Add `flexShrink:0`
- Divider div: add `flexShrink:0`
- Map buttons:
  - Change `"United States Map"` to `"US Map"`
  - Change `"New York City Map"` to `"NYC Map"`
  - Add a third button after NYC Map: `<button onClick={onGoHospital} style={{...mapBtnBase,background:hospitalActive?RED:"transparent",color:hospitalActive?"#fff":"#888"}}>Health Center</button>`
- Breadcrumbs:
  - Remove the `{level===2&&(...)}` breadcrumb block entirely (Health Center is now a tab button)
  - Change `{level===3&&(...)}` block to only show Story (not Health Center + Story):
    ```jsx
    {level === 3 && (
      <span style={{display:"flex",alignItems:"center",gap:6,marginLeft:8,flexShrink:0}}>
        <span style={{color:"rgba(255,255,255,0.3)",fontSize:14}}>›</span>
        <span style={{fontFamily:"'Libre Franklin',sans-serif",fontSize:14,fontStyle:"italic",color:RED,fontWeight:600}}>Story</span>
      </span>
    )}
    ```
    Note: marginLeft is 8 (was 4), fontSize for "Story" is 14 (was 13), and only the Story span is present (no Health Center span).

**3. App component — update NavBar invocation:**

Change the NavBar JSX in the App return from:
```jsx
<NavBar level={level} onHome={()=>zoomTo(-1)} onGoUS={()=>level!==0&&zoomTo(0)} onGoNYC={()=>level<1&&zoomTo(1)}/>
```
to:
```jsx
<NavBar
  level={level}
  onHome={()=>zoomTo(-1)}
  onGoUS={()=>level!==0&&zoomTo(0)}
  onGoNYC={()=>level!==1&&zoomTo(1)}
  onGoHospital={()=>level!==2&&zoomTo(2)}
/>
```
Note: onGoNYC changed from `level<1` to `level!==1` (allows backward navigation from deeper levels). Added onGoHospital prop.
  </action>
  <verify>
    <automated>cd /home/jaeho/civic-health-alliance && npx --yes eslint src/App.jsx 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep 'x={2.5}' src/App.jsx returns 2 matches (floor markers)
    - grep 'fontSize:1.8' src/App.jsx includes the department label font size line
    - grep 'fontSize:2.8' src/App.jsx includes the department metric font size line
    - grep 'fontSize:1.3' src/App.jsx includes the department metricLabel and note font sizes
    - grep 'width={70}' src/App.jsx returns match in hover tooltip rect
    - grep 'p.x-35' src/App.jsx returns match in hover tooltip rect x
    - grep 'p.y-18' src/App.jsx returns match in hover tooltip rect y
    - grep 'p.y-13' src/App.jsx returns match in hover tooltip name text y
    - grep 'fontSize:1.4' src/App.jsx includes the hover role text font size
    - grep 'fill="rgba(26,26,26,0.95)"' src/App.jsx returns match (updated opacity from 0.93)
    - grep 'onGoHospital' src/App.jsx returns at least 3 matches (prop destructuring, button onClick, App invocation)
    - grep 'Civic Health"' src/App.jsx returns 1 match (shortened brand name, not "Civic Health Alliance")
    - grep '"US Map"' src/App.jsx returns 1 match (shortened from "United States Map")
    - grep '"NYC Map"' src/App.jsx returns 1 match (shortened from "New York City Map")
    - grep '"Health Center"' src/App.jsx returns 1 match (new tab button)
    - grep 'hospitalActive' src/App.jsx returns at least 2 matches
    - grep 'level===2' NavBar breadcrumb block is gone (no "Health Center" breadcrumb at level 2)
    - grep 'onTouchEnd' src/App.jsx returns at least 4 matches (Phase 3 touch handlers preserved)
    - grep 'fill="transparent"' src/App.jsx returns at least 4 matches (Phase 3 touch targets preserved)
    - grep 'overflowX:"auto"' src/App.jsx returns 1 match (NavBar overflow handling)
    - grep 'level!==1&&zoomTo(1)' src/App.jsx returns 1 match (onGoNYC allows backward nav)
    - grep 'level!==2&&zoomTo(2)' src/App.jsx returns 1 match (onGoHospital)
    - npm run build succeeds with no errors
    - npx eslint src/App.jsx produces no errors
  </acceptance_criteria>
  <done>HospitalScene uses remote font sizes and tooltip styling with Phase 3 touch infrastructure intact. NavBar has three tab buttons, shortened labels, overflow handling, and Story-only breadcrumb at level 3. App passes onGoHospital and uses bidirectional onGoNYC. Build and lint pass.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full merge:

1. `npm run build` succeeds (no syntax errors, no import issues)
2. `npx eslint src/App.jsx` produces no errors
3. All four HOSPITAL_PEOPLE entries have summary fields (PersonStory callout works)
4. All four people have coordinates matching their room centers (29/71 for x, 38/71 for y)
5. DEPARTMENTS uses remote's widened geometry (w:36 for all rooms)
6. Touch targets and touch handlers from Phase 3 are present
7. NavBar has onGoHospital prop and three tab buttons
</verification>

<success_criteria>
- src/App.jsx builds and lints without errors
- Remote content adopted: shortened names, punctuation changes, department labels, Chapter 3 text
- Remote geometry adopted: w:36, x:11/53, reordered bottom row
- Remote styling adopted: smaller font sizes, wider tooltip, floor markers at x=2.5
- Remote NavBar adopted: three tabs, shortened labels, overflow handling, onGoHospital
- Local Phase 1 preserved: all four summary fields present
- Local Phase 3 preserved: touch target rects, onTouchEnd/onTouchCancel handlers
- Person coordinates recalculated for new geometry: dr-saffo (29,38), patient-one (71,38), patient-two (29,71), patient-three (71,71)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-address-remote-and-local-conflicts/03.1-01-SUMMARY.md`
</output>
