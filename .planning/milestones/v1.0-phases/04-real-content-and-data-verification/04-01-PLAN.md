---
phase: 04-real-content-and-data-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.jsx]
autonomous: true
requirements: [DATA-01, DATA-03]

must_haves:
  truths:
    - "Every US state voterTurnout value in STATE_DATA matches the UF Election Lab 2020 VEP turnout rate"
    - "Source comment block above STATE_DATA documents voterTurnout source (UF Election Lab), uninsured source (KFF ACS), and lifeExp source (CDC NVSR) with URLs, denominators, election cycles, and year"
    - "Level 0 citation links display correct source names and link to correct URLs"
  artifacts:
    - path: "src/App.jsx"
      provides: "Corrected STATE_DATA voterTurnout values and methodology comments"
      contains: "voterTurnout:61.5"
  key_links:
    - from: "STATE_DATA.voterTurnout"
      to: "engagementColor()"
      via: "USMap renders hex polygons colored by engagementColor(d.voterTurnout)"
      pattern: "engagementColor\\(d\\.voterTurnout\\)"
    - from: "STATE_DATA.voterTurnout"
      to: "LeftPanel hover card"
      via: "hovStateData.voterTurnout displayed in stat row"
      pattern: "hovStateData\\.voterTurnout"
---

<objective>
Replace all 51 voterTurnout values in STATE_DATA with verified 2020 VEP turnout rates from the UF Election Lab, add a source methodology comment block above STATE_DATA, and update the Level 0 LeftPanel citation links to reference correct sources.

Purpose: The current voterTurnout values are systematically wrong (VAP-based or prior election cycle), off by 1-7 percentage points. California shows 62.1% but the correct 2020 VEP value is 68.8%. This materially misrepresents the correlation the visualization is built around. The engagementColor breakpoints (70/64/58) must NOT change -- the corrected data is more accurate and the visual shift is intentional.

Output: Corrected STATE_DATA block with verified values, methodology comments satisfying DATA-03, and accurate citation links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-content-and-data-verification/04-RESEARCH.md
@src/App.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source methodology comment block above STATE_DATA</name>
  <files>src/App.jsx</files>
  <read_first>src/App.jsx (lines 10-65, the current STATE_DATA block and preceding comment)</read_first>
  <action>
Insert a methodology comment block immediately above `const STATE_DATA = {` (after the existing `DATA` section header comment, before line 13). The comment must contain exactly this text:

```
/* --- STATE_DATA -----------------------------------------------------------
   voterTurnout  -- 2020 General Election VEP Turnout Rate (%)
                    Source: UF Election Lab (Michael McDonald), v1.2
                    URL: https://election.lab.ufl.edu/voter-turnout/2020-general-election-turnout/
                    Denominator: Voting-Eligible Population (citizens 18+, excluding ineligible felons)
                    Election cycle: November 2020 Presidential General Election
   uninsured     -- Non-elderly (ages 0-64) uninsured rate (%)
                    Source: KFF State Health Facts, ACS-based estimates
                    URL: https://www.kff.org/state-category/health-coverage-uninsured/
                    Year: 2022/2023 ACS
   lifeExp       -- Life expectancy at birth, total population (years)
                    Source: CDC NCHS, U.S. State Life Tables, 2022
                    Report: NVSR Vol. 74, No. 12 (December 4, 2025), Table A
                    URL: https://www.cdc.gov/nchs/data/nvsr/nvsr74/nvsr74-12.pdf
   ------------------------------------------------------------------- */
```

Do NOT change any data values in this task. Do NOT change the `const STATE_DATA` line. Only add the comment block.
  </action>
  <verify>
    <automated>grep -c "UF Election Lab" src/App.jsx | grep -q "1" && grep -c "KFF State Health Facts" src/App.jsx | grep -q "1" && grep -c "NVSR Vol. 74" src/App.jsx | grep -q "1" && echo "PASS: all three source references present" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx contains the string "UF Election Lab (Michael McDonald), v1.2"
    - src/App.jsx contains the string "KFF State Health Facts, ACS-based estimates"
    - src/App.jsx contains the string "NVSR Vol. 74, No. 12 (December 4, 2025), Table A"
    - src/App.jsx contains the string "Voting-Eligible Population (citizens 18+, excluding ineligible felons)"
    - src/App.jsx contains the string "November 2020 Presidential General Election"
    - src/App.jsx contains the string "2022/2023 ACS"
    - The comment block appears BEFORE `const STATE_DATA = {`
    - `npm run build` succeeds (no syntax errors from the comment)
  </acceptance_criteria>
  <done>Source methodology comment block is present above STATE_DATA documenting all three metrics with source, URL, denominator, and year</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 51 voterTurnout values and update Level 0 citations</name>
  <files>src/App.jsx</files>
  <read_first>src/App.jsx (lines 13-65 for STATE_DATA values; lines 340-345 for Level 0 citation links), .planning/phases/04-real-content-and-data-verification/04-RESEARCH.md (the verified data table)</read_first>
  <action>
**Part A: Replace all 51 voterTurnout values in STATE_DATA**

Replace each state's `voterTurnout` value with the correct 2020 VEP value. The exact mapping (state code -> new value):

AL:61.5, AK:68.2, AZ:66.6, AR:56.2, CA:68.8, CO:76.7, CT:70.1, DE:68.3, DC:68.9, FL:70.8, GA:67.1, HI:55.4, ID:64.9, IL:66.5, IN:61.1, IA:72.0, KS:65.1, KY:64.4, LA:64.1, ME:74.9, MD:69.5, MA:71.3, MI:73.3, MN:79.2, MS:60.6, MO:65.5, MT:71.3, NE:69.1, NV:64.3, NH:74.0, NJ:72.0, NM:60.5, NY:61.7, NC:71.0, ND:63.1, OH:67.1, OK:54.4, OR:75.3, PA:69.9, RI:63.1, SC:64.7, SD:65.6, TN:59.2, TX:59.8, UT:67.2, VT:71.3, VA:71.8, WA:75.3, WV:57.2, WI:75.0, WY:64.7

Do NOT change `name`, `uninsured`, or `lifeExp` values -- only `voterTurnout`.
Do NOT change the `engagementColor` function breakpoints (70/64/58). The upward shift in VT values causing more green states is an intended consequence of fixing the data.

**Part B: Update Level 0 citation links**

Find the citation links block at approximately lines 341-344 (inside `{level===0&&(` legend card, after the color legend). Replace the three `<a>` tags with:

1. Voter Turnout link:
   - href: `https://election.lab.ufl.edu/voter-turnout/2020-general-election-turnout/`
   - text: `Voter Turnout -- UF Election Lab, 2020 VEP`

2. Uninsured Rate link:
   - href: keep existing `https://www.kff.org/state-health-policy-data/state-indicator/total-population/` (already correct URL)
   - text: `Uninsured Rate -- KFF ACS, 2022-2023`

3. Life Expectancy link:
   - href: keep existing `https://www.cdc.gov/nchs/data/nvsr/nvsr74/nvsr74-12.pdf`
   - text: `Life Expectancy -- CDC NVSR Vol. 74 No. 12, 2022`
  </action>
  <verify>
    <automated>grep "voterTurnout:61.5" src/App.jsx && grep "voterTurnout:68.8" src/App.jsx && grep "voterTurnout:55.4" src/App.jsx && grep "voterTurnout:79.2" src/App.jsx && grep "UF Election Lab, 2020 VEP" src/App.jsx && grep "KFF ACS, 2022" src/App.jsx && grep "CDC NVSR Vol. 74 No. 12, 2022" src/App.jsx && npm run build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/App.jsx AL entry contains `voterTurnout:61.5` (was 58.9)
    - src/App.jsx CA entry contains `voterTurnout:68.8` (was 62.1)
    - src/App.jsx HI entry contains `voterTurnout:55.4` (was 50.3)
    - src/App.jsx MN entry contains `voterTurnout:79.2` (was 76.4)
    - src/App.jsx NH entry contains `voterTurnout:74.0` (unchanged, already correct)
    - src/App.jsx ND entry contains `voterTurnout:63.1` (unchanged, already correct)
    - src/App.jsx contains zero instances of the string `voterTurnout:58.9` (old AL value gone)
    - src/App.jsx contains zero instances of the string `voterTurnout:62.1` (old CA value gone -- note SC also had 62.1 but is now 64.7)
    - src/App.jsx contains the citation text "UF Election Lab, 2020 VEP"
    - src/App.jsx contains the citation text "KFF ACS, 2022-2023"
    - src/App.jsx contains the citation text "CDC NVSR Vol. 74 No. 12, 2022"
    - src/App.jsx contains the href "https://election.lab.ufl.edu/voter-turnout/2020-general-election-turnout/"
    - src/App.jsx no longer contains the string "MAP, 2024"
    - src/App.jsx no longer contains the href "lgbtmap.org"
    - `npm run build` exits 0
    - `npm run lint` exits 0
    - The `engagementColor` function still contains thresholds 70, 64, 58 (unchanged)
  </acceptance_criteria>
  <done>All 51 voterTurnout values match the UF Election Lab 2020 VEP dataset exactly, Level 0 citations reference correct sources with correct URLs, and the build succeeds</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` succeeds
2. `npm run lint` passes
3. All 51 voterTurnout values match the research table (spot check: AL=61.5, CA=68.8, HI=55.4, MN=79.2, NH=74.0, ND=63.1, WY=64.7)
4. Source comment block is present above STATE_DATA with all three metric sources documented
5. Level 0 citation links reference UF Election Lab, KFF ACS, and CDC NVSR with correct URLs
6. engagementColor breakpoints remain 70/64/58 (not adjusted)
7. No changes to lifeExp, uninsured, or name fields in STATE_DATA
</verification>

<success_criteria>
- DATA-01 partially satisfied: All US state stats match cited primary sources (voterTurnout corrected; lifeExp and uninsured already correct per research)
- DATA-03 partially satisfied: STATE_DATA methodology is documented in code comments with source, URL, denominator, election cycle, and year for all three metrics
- Level 0 citation links are accurate
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-content-and-data-verification/04-01-SUMMARY.md`
</output>
